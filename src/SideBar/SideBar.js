import React from 'react';
import './SideBar.css';

const SideBar = () => {
    return (
        <aside className="sidebar">
            <button>Save</button>
            <button>Load</button>
        </aside>
    );
};

export default SideBar;
